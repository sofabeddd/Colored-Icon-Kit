#pragma once
#include <Geode/Geode.hpp>

using namespace geode::prelude;

bool isAprilFools();

namespace variables {
    extern bool update_installed;
    extern int page_number;
    extern IconType icon_type;
}